# LaRicia Leach
# 11/4/2024
# P4LAB2
# Use loops to show only positive time tables

'''
# Get an integer from user
# Determine if integer is positive or negative
# If positive, display table
print("Show times tablle if user enters positive integer")
# If integer is negative, tell user no can do
run_again = input("Would you like to run the program again? ")
print("Program has ended...")
'''
run_again  =  "yes"

while run_again != "no":

    user_num = int(input("Enter an integer:"))

    if user_num >= 0:
        #display multipliaction for that value and range (1-12)
        for item in range(1, 13):
            print(f"{user_num} * {item} = {user_num} * {item}")
    else:
        print("This program does not handle negative values")

    run_again = input("Would you like to run program again?")

# Loop has been broken! User entered 'no'
print("Program is ending...")

